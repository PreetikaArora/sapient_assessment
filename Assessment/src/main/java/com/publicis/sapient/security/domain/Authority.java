package com.publicis.sapient.security.domain;

import com.publicis.sapient.user.domain.User;

/**
 * Authorities that can be granted to a {@link User}.
 *
 *
 */
public enum Authority {
    ADMIN, USER
}
